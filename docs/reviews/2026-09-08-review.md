# 序时账可选筛选验收记录

2026年9月8日，按用户确认的两项序时账需求审查。

## 完成的行为

桌面两个复选框默认关闭。启用后用实际文件列名和科目显示设置。入账时间筛选包含夜间、普通周末、法定节假日与调休放假，周末补班按工作日。根据账中年份匹配2024至2026年的已核验日历，支持离线运行及项目日历更新；缺年或读取失败明确显示未检查。

拆分审批按选择的科目和借贷方向读取原始金额。近限额范围由用户输入，按同账套、同日、同对方归集候选。缺少归集资料仅列金额候选，红字不取绝对值或默默抵减。组金额重复展示须避免再次相加。

两个CLI和桌面处理共用筛选函数。精简CLI明确启用时追加筛选表，未启用时保持原有输出。读取器保存来源文件和首个工作表名称，用原始行索引追溯；Excel日期精度在夜间筛选启用后读取。

## 实际验证

- 原基线7项通过。
- 新用例先因筛选模块或命令行参数缺失失败，随后实现通过。
- 当前20项测试全部通过，覆盖休息日和补班、未知年、日期精度、红字、金额归集、非法参数、日历更新失败保留原文件、设置窗口及完整GUI操作链。
- 修改前源代码由202554b0426eb4fdb9e27e9f09082c2f37b0d859提取，在独立目录生成三个基线工作簿。修改后主程序完整模式、独立CLI精简和完整模式逐格对照一致（沿用原比较器允许首行对齐差异，另以当前默认样例检查）。
- 已验证原有资料缺列时退出码仍为1。
- Python源码编译和中文编码检查通过。
- 最终随附EXE完成精简、完整两种模式的原版输出对照，均通过；两种模式也实际验证新增功能，均选中6条时间记录和4条付款候选，金额符合手算结果。正式覆盖前已备份旧EXE并校验哈希。
- 最终EXE在上述补充修正后重新构建，SHA256为EEAF3A5ACD34CF3C4697136E922BB96853559973906C08405CDEE5C9F1131139；覆盖仓库文件后再次通过原版输出对照及精简、完整模式的新增功能验收。

## 审查处理

科目选择集合只创建一次，避免每行重建。日期列不自动套用会计月；仅日期单元格不冒充实际午夜。日历更新先校验再替换，原缓存存在时先备份并校验。原始工作表来源已补齐。补测带地区标记的Excel日期格式，修正将zh-CN中的h误当小时的情况，日期精度判断先排除地区标记。没有增加审批人权限检查或其他已删除功能。

联网验收发现中文文件名下载链接未编码，已补回归用例并修正。真实访问本项目公开日历地址成功，下载的2024至2026年数据与随附数据一致，实际执行下载、校验及保存。桌面自动验收改为等待界面显示处理完成后再打开工作簿，避免在写出尚未结束时抢读文件。

本次未改对方科目算法及原有分析规则。结果是候选记录，是否属于同一业务、是否构成拆单仍须结合原凭证判断。
