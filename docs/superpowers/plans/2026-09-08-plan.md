# 可选的入账时间和拆分审批筛选实施计划

按需求工作流的 executing-plans 在当前独立克隆中串行执行。用户已授权后续决策、提交和推送，无子agent。

目标为可选的入账时间和拆分审批筛选。设计见 ../specs/2026-09-08-design.md。Python及现有依赖，Windows入口保持不变。

涉及文件为 src/io/reader.py、src/io/writer.py、src/gui/app.py、src/pipeline/orchestrator.py、main.py、cli/序时账分析器命令行版.py；新增 src/pipeline/凭证筛选.py、src/gui/筛选设置.py、src/日历.py、src/中国节假日.json、tests/test_凭证筛选.py，同步更新README及必要打包数据配置。

- [x] 编写 tests 下新增功能测试，运行当前仓库的 `python -X utf8 -m pytest -q`，确认因新功能缺失而失败。
- [x] 实现共享检查函数和数据字段，再运行新增测试，预期全部通过。run_screening(df, options)返回工作表名到DataFrame的映射，关闭时返回空映射；读取器在attrs中保留来源及时间精度，避免污染原输出。
- [x] 接入原有GUI和CLI写出入口。用合成PDF或Excel从入口运行，回读工作簿，核验金额、来源、日期判断以及关闭时兼容性。
- [x] 更新README、日历更新说明或输出说明，运行全量测试与源码编译检查。
- [x] 对照需求自行审查，补失败用例修正发现的问题，记录 docs/reviews/2026-09-08-review.md。
- [x] 复核远端无冲突和工作区。本次交付按用户授权提交并推送原默认分支，最终提交号由交付回复报告。

金额示例断言为 `assert payment == Decimal('98000')`，不能断言借贷合计196000。日期示例断言为1月4日补班不因周末命中。银行金额示例断言为975减75等于900。具体可执行用例随第一步落入测试文件，先观察失败后改代码。
