# 全面独立复核：实施计划

复核基准：origin/main @ 4b4c740。工作目录 `C:\Users\27651\Documents\Code\序时账分析器`。Python 用 `C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe`。

## 任务清单（按依赖排序）

### 任务 1：建立验证基线
- 命令：`python -m pytest tests/ -q`（在仓库根目录）
- 成功标准：现有全部测试通过；记录通过数。有失败先确认是代码问题还是环境问题。
- 同时核实事实：规则库实际条数（README 称 256 条）；`cli/ledger-cli.exe` 实际大小（README 称 32,346,279 字节）；README §1 示例的"标准模板"匹配类型是否真实命中规则库。

### 任务 2：静态复核清单逐文件过一遍并记录结论
覆盖：main.py、make_excel.py、cli/序时账分析器命令行版.py、src/core/*、src/pipeline/*、src/io/*、src/gui/*、src/日历.py、src/utils/logger.py、tests/*、README.md、CONTEXT.md。
产出：每文件"通过/问题/待判断"结论，问题按严重程度分级。

### 任务 3：可疑点探针实测
对任务 2 中标记的疑点写临时探针脚本（放 %TEMP%，不进仓库）实测行为，区分"确认 bug"与"误报"。

### 任务 4：TDD 修复确认的 bug
每个修复：RED（失败测试入 tests/）→ GREEN（最小修复）→ VERIFY（定向测试 + 全量 pytest）。
修改已有文件前统一备份到 `C:\Users\27651\BackUp\序时账分析器_20260924_*\`。

### 任务 5：README 同步
- 修正与代码不符的描述（已知候选：§3.1 操作步骤、§9.2 算法章节的剪枝表与伪代码、§5 工作表数量表述，按任务 1-3 结果增删）。
- 更新日志新增本次复核/修复条目；版本号按实际修复定（有代码修复 → v2.2.1 或更高）。
- 注明 cli/ledger-cli.exe 未随源码重建（用户已确认本次不重建）。

### 任务 6：复核报告
写入 `docs/reviews/2026-09-24-全面独立复核-review.md`：范围、发现、修复、验证证据、残余风险。

### 任务 7：交付
`git status` / `git diff` 核对 → 提交（中文提交信息）→ 推送 origin/main → 报告提交号。

## 风险点

- GUI 测试（test_筛选界面.py）需要桌面环境；本机为 Windows 桌面可运行，若失败先区分环境/代码原因。
- 多进程测试在 Windows 上走 spawn，耗时较长，属预期。
- 若任务 1 基线就有测试失败，停止修复新内容，先定位基线失败根因。
