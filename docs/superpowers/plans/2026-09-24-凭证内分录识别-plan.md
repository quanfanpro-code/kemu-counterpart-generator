# 凭证内分录识别实施计划

> 实施时先阅读本计划与同日设计稿，串行执行每项任务；每项先写失败的行为测试，再做最小实现和验证。用户已授权本任务自主实施、同步 README、提交并推送；不启动子代理。

**目标：**在不改现有对方科目匹配阶段的前提下，识别单张凭证中可按原始行次划分的多个连续、严格借贷平衡分录组。

**架构：**共同的 `process_group` 入口先检查原始凭证；只有完整、连续、严格平衡且满足保护条件的多个组才分别调用原有 `GroupProcessor.process()`。其余凭证仍一次调用原处理器。

**技术：**Windows、本机 Python 3.14、pandas、pytest、既有 `PrecisionEngine`；不增加依赖。

**依据：**`docs/requirements/2026-09-24-凭证内分录识别-requirement.md` 与 `docs/superpowers/specs/2026-09-24-凭证内分录识别-design.md`。

## 全局约束

- 仅处理本仓库；已有文件修改前备份到 `C:\Users\27651\BackUp\序时账分析器_年月日_时分秒\` 并校验；中文文件保持 UTF-8，优先 BOM。
- 不改 `GroupProcessor` 的数据准备、结转损益、标准模板、金额匹配、兜底拆分规则、精度容差或既有匹配类型。
- 不改变桌面、主命令行、独立命令行接口，不添加输入列、结果列或工作表。
- 保护多条利润科目、红字、同行借贷双非零、零金额、空科目、不平衡凭证；无法拆组时完整走旧流程。
- 同一凭证的全部代码修改由当前实例串行完成；测试期间不覆盖用户原始账簿。

## 任务一：建立行为基线与失败测试

**文件：**新建 `tests/test_凭证内分录识别.py`，只读 `src/pipeline/group_processor.py`。

**接口：**测试直接调用 `process_group((凭证键, DataFrame))`；复用现有输出字段 `一级科目`、`对方科目`、`匹配类型`、`借方发生额`、`贷方发生额`、`_orig_idx`。

- [ ] 记录当前提交及工作树状态，运行现有测试，保存旧版普通单分录、纯结转、多利润、红字及异常行样例的精确输出基线。
- [ ] 写失败的正常路径测试：输入按行次“借甲100、贷乙60、贷丙40、借丁60、贷戊60”，断言甲只与乙/丙对应、丁只与戊对应；运行 `C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe -B -X utf8 -m pytest -q tests/test_凭证内分录识别.py`，确认测试因现有跨组配对而失败。
- [ ] 写失败的混合结转测试：输入“借本年利润100、贷主营业务收入100、借管理费用50、贷银行存款50”，断言管理费用不指向本年利润；运行同一测试文件并确认失败原因正确。
- [ ] 加入保护样例的旧结果断言：无内部平衡边界、含多条利润科目、红字、同行双非零、空科目/零额、不平衡、仅一个平衡组及同金额重复；这些样例在实现前应保持现有行为。

示例断言结构：

```python
rows, warnings = process_group((('2026-01', '记', '1'), source_df))
assert {(r['_orig_idx'], r['对方科目']) for r in rows if r['一级科目'] == '丁'} == {(3, '戊')}
```

## 任务二：在共同入口添加有界识别与原流程回退

**文件：**修改 `src/pipeline/group_processor.py`，继续运行 `tests/test_凭证内分录识别.py`。

**接口：**新增模块内私有函数 `_split_contiguous_balanced_groups(group: pd.DataFrame)`，返回连续原始 DataFrame 片段列表，或返回 `None` 表示完整走旧流程；保留 `process_group` 既有返回类型 `(输出行, 警告列表)`。

- [ ] 复查失败测试覆盖的边界，确认没有依赖匹配类型的偶然排序。
- [ ] 仅在 `process_group` 入口接入识别：对非负、单边发生且科目有效的原始行，用 `PrecisionEngine.to_integer_li` 累计借减贷；遇净额严格为零截出完整组；仅当全部行恰好形成至少两组时拆开。多条符合现有 `_is_profit_subject` 判定的条目直接回退。
- [ ] 对新识别步骤的意外异常保留原凭证处理并返回可定位提示；不能吞掉原匹配算法自身的异常。
- [ ] 运行新测试，确认正常、混合结转与全部回退样例通过；再运行完整 pytest。

入口期望形态：

```python
processor = GroupProcessor(group_data)
segments = _split_contiguous_balanced_groups(processor, group_data[1])
if segments is None:
    return processor.process()
results = [GroupProcessor((group_data[0], segment)).process() for segment in segments]
return [row for rows, _ in results for row in rows], [w for _, warns in results for w in warns]
```

实际实现可消除中间列表，但不得改旧 `GroupProcessor.process()` 的处理顺序。

## 任务三：入口、下游结果、说明和交付验证

**文件：**补充 `tests/test_凭证内分录识别.py`；修改 `README.md` 和 `cli/ledger-cli-README.md`；新增 `docs/reviews/2026-09-24-凭证内分录识别-review.md`。

- [ ] 写入口行为测试：用临时 Excel 分别从 `main.py --no-gui`、独立命令行 summary/full 调用，回读结果核对多分录关系、未拆旧结果、原行/发生额守恒及结果列；运行现有桌面端到端测试并核对桌面入口调用的共同流水线。
- [ ] 写串行/并行一致性测试：构造超过 `SMALL_INPUT_SERIAL_THRESHOLD` 的独立凭证；比较对方科目、金额、行序和失败分组。
- [ ] 在 README 和独立命令行说明中说明新增识别范围、行序推断的局限、回退情形和既有规则不变；写前完成备份与哈希核验。
- [ ] 运行完整 pytest、真实入口回归及必要的构建检查；审查纯结转、多利润、红字、普通单分录、异常分析、班福和可选筛选；记录验证命令、通过数和残余限制。
- [ ] 审查需求、设计、计划与实现是否一致；必要时用失败测试驱动修复，完成审查记录并再次运行最终验证。
- [ ] 检查 Git 状态、差异、中文编码和备份证据；获取远程最新状态，只有无冲突且验证通过时，提交本任务文件并推送到原有 `origin/main`，随后再次核对本地与远程提交一致。

关键入口验证命令形态：

```powershell
& 'C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe' -B -X utf8 -m pytest -q
& 'C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe' -B -X utf8 -m pytest -q tests/test_凭证内分录识别.py tests/test_筛选界面.py
& 'C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe' -B -X utf8 -m pytest -q
```

本机已确认 PyInstaller 6.22.3 可用。正式交付前按 cli_build.spec 重建 EXE，先备份并校验已有 cli\\ledger-cli.exe，再用新 EXE 的真实入口验证精简及完整模式；不能把旧 EXE 当作新代码验收结果。